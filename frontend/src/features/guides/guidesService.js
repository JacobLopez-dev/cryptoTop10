import axios from 'axios'

const API_URL = '/api/guides/'

const createGuide = () => {
    console.log('create guide service')
}

const guidesService = {
    createGuide,
}

export default guidesService